<h1>Review creation</h1>

<form action="{{ path('admin_ajax_qcm_review_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_ajax_qcm_review') }}">
            Back to the list
        </a>
    </li>
</ul>
